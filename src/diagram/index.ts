/** RISC-V accelerator data-path diagram — full interactive version lands in
 *  Phase 3. Until then the mount renders nothing (the section reads fine
 *  without it). */

export function mountDiagram(el: HTMLElement | null): void {
  if (!el) return;
  // Phase 3: SVG schematic + transaction stepper + benchmark mode.
}

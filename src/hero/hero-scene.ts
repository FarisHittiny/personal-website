/** WebGL hero scene — implemented in Phase 4. This stub keeps the dynamic
 *  import target valid; it intentionally does nothing yet, leaving the
 *  static fallback in place. */

export function startHeroScene(_el: HTMLElement): void {
  // Phase 4: three.js lattice replaces the fallback here.
}
